# 安全与数据边界

## 运行范围

这是面向单个可信操作系统用户的本地投标工作台。官方启动方式只监听 `127.0.0.1`，没有多用户登录、租户隔离或互联网访问控制。不要将服务改为监听公网或局域网，也不要通过反向代理、端口转发或隧道公开本机接口。拥有本机进程权限的其他程序仍可能访问本地服务。

浏览器请求有 Host、Origin 和跨站检查，响应设置 CSP、禁止嵌入和禁止 MIME 猜测。这些措施不能代替身份认证。请保留文件解析安全规则，并仅导入有权使用且来源可信的文件；文件解析器和本机办公软件不属于安全沙箱。

## 数据与模型

- 原始文件副本、解析正文、项目、导出文件和任务记录保存在本机数据目录。业务资料不是加密数据库，请用操作系统权限和磁盘加密保护，并谨慎保存备份。
- Windows 页面保存的模型密钥使用当前用户的 DPAPI 加密；接口只返回是否已配置，不返回密钥明文。非 Windows 环境通过 `DEEPSEEK_API_KEY` 环境变量提供密钥。环境变量也需要妥善保护。
- 点击 AI 分析、生成、重新生成、复核或模型连接测试时，相关材料可能发送到 DeepSeek 官方接口并产生费用。本地存储不代表模型推理离线。上传和正文编辑本身不会自动调用模型。
- Langfuse 遥测默认关闭。启用后会向自行配置的本机接收端发送任务与模型提示词、回答等内容；不要在不了解数据范围时启用或转发该接收端。
- 模型回答、附件文字和网页链接都不是可信指令。人工批准仅表示操作人认可本次内容，不等于第三方证明、签章、附件齐备或正式投标合规。

## 源码与个人工作区分离

公开仓库不包含真实企业资料、客户原件、项目数据库、模型响应、密钥、个人配置或旧工作区 Git 历史。示例与测试材料为合成内容。运行后不要把 `data/`、导出文件、`.env`、`.langfuse.env`、调试日志或私有截图提交到 Git。

测试默认使用临时数据库、移除继承的模型密钥并阻止外部网络连接。前端逻辑测试需要 PATH 中有 Node.js；没有时会明确标记跳过。测试中的分页替身不代表真实 Word 或 PDF 排版验证。

## 报告问题

普通使用问题可提交 GitHub Issue，说明版本、复现步骤与脱敏后的报错。不要在公开 Issue、PR 或截图中粘贴密钥、报价、合同、个人身份材料或真实招标正文。

发现可能暴露数据或绕过访问边界的问题时，请优先使用仓库 Security 页面的私密漏洞报告入口（维护者启用后可用）。若尚未启用，先提交不含攻击细节和敏感数据的联系请求。无法证明风险已解决前，不要公开真实数据或可复用凭据。

本项目没有宣称经过独立安全审计。维护者会根据可复现信息处理问题；公开测试通过不构成对所有文件、模型输出或部署环境的安全保证。
