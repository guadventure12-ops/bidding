# R022-public.2 发布验证

验证日期：2026-09-16。发布对象是独立清理后的源码快照，不包含此前私有工作区的Git历史。产品验收版本显示为R022；精确版本以Git提交与运行界面的构建指纹为准。

## 已执行

| 检查 | 实际结果 |
| --- | --- |
| 全新安装 | Python 3.12.10，新建项目`.venv`，从PyPI安装core+dev；无vendor或开发助手运行时依赖。 |
| 普通完整测试 | **1347项测试通过，17项子测试通过**；模型和普通分页使用替身，无真实模型请求。两项上游测试客户端弃用警告不影响结果。 |
| 独立启动 | 新数据目录、仅本机随机端口，健康接口、项目列表与目录接口正常；未填密钥。 |
| 合成演示 | 创建3个一级分类、6个独立二级章节；重复指定已有目录被拒绝，未覆盖已有内容。 |
| 实际导出 | 通过公开版API将合成项目导出为**6页PDF**并保留同源DOCX；使用本机Microsoft Word，未使用分页替身。 |
| 可选OCR | 另建安装验证core+OCR，OCR引擎本地初始化及空白图处理成功；不代表对任意扫描件的识别质量保证。 |
| 依赖一致性 | `pip check`通过。 |
| 已知漏洞检查 | 对实装core+OCR的34个依赖执行pip-audit，未发现已知漏洞；结果仅对应检查当日的版本和漏洞数据库。 |
| README | 使用GitHub Markdown渲染器核对；四张图片来自合成数据下实际界面，不含真实客户资料。 |

完整普通测试使用Windows、Python3.12与PATH中的Node.js。GitHub CI使用Windows/Python3.12/Node.js22，并采用相同的离线测试入口。普通测试中的模型替身、模拟页码不能代替真实AI质量或原生排版验收，上表分别列出已执行范围。

## 本次发布整理

- 默认企业名、材料路径、密钥和项目为空；去除旧品牌、客户标识和个人开发路径。
- 用标准Python虚拟环境启动，分开core、OCR和开发依赖，锁定本次实装的直接依赖版本。
- 保留知识库批量批准、产品模块选材、一二级目录、单节操作、审核及导出功能。
- 修复连续Markdown目录行及评分标题的目录范围误判，保留原始来源定位；评分表的结构化推荐仍优先使用DOCX。
- 校正历史测试与现行正文/内部提示分离规则不一致的断言，未为测试清除真实缺项或放宽批准规则。两项只测试非公开生产修复脚本的历史用例不属于本发布包。

## 上传范围

仅包含应用源码、合成测试、启动与安装文件、MIT及第三方说明、产品文档、合成素材和四张演示截图。不包含真实企业资料、招标原件、报价、合同、数据库、密钥、模型回答、私有Git历史、运行缓存和第三方二进制文件。

上传前按明确文件清单检查旧标识、个人路径、已知真实项目标识、常见令牌与私钥形态，并核对暂存内容的SHA256。此类筛查不是独立安全审计，不保证排除所有安全问题。详见[安全边界](../SECURITY.md)。

## 尚未验证或不提供

- 本轮没有发起付费模型调用，没有重新分析或生成真实客户投标书，未验证任意模型账户与长篇生成质量。
- 不提供公网多用户部署、电子签章、投标平台加密或自动递交能力。
- 完整原生分页以Windows桌面版Microsoft Word为依赖，不把其他操作系统列为完整流程已验证环境。
- MIT只许可原创源码，第三方依赖须遵守自身许可证，尤其PyMuPDF的AGPL/商业许可边界。

## R022-public.2

首次GitHub检查中，1346项通过，1项因Windows短路径与完整路径字符串不同而失败；两条路径实际指向同一导出文件。本版只将该测试改为比较规范化的绝对路径，应用导出行为未改变。请以此提交对应的Actions结果核对云端测试状态；原版本与检查记录保留。
